import { StyleSheet } from "react-native";

export default StyleSheet.create({
    one: {
      alignItems: 'center',
      justifyContent: 'center',
    },
    two: {
      fontSize: 26,
      fontWeight: 'bold',
    }
})